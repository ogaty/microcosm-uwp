<Page
    x:Class="microcosm.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:microcosm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <CommandBar IsSticky="True" IsOpen="True">
            <CommandBar.Content>
                microcosm pro 1.0
            </CommandBar.Content>
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="ファイル" Icon="OpenFile"></AppBarButton>
                <AppBarButton Label="設定" Icon="Setting"></AppBarButton>
                <AppBarButton x:Name="Copyright" Label="バージョン" Icon="Help" Click="Copyright_Click"></AppBarButton>
            </CommandBar.PrimaryCommands>
            <CommandBar.SecondaryCommands>

            </CommandBar.SecondaryCommands>

        </CommandBar>

        <StackPanel Margin="0,60,0,0">
            <TextBlock Text="abc" Name="NatalName"></TextBlock>
            <Button Content="Button" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Button_Click"/>
        </StackPanel>

    </Grid>
</Page>
